schemaVersion: 2.2.0
attributes:
  space:
    instanceType: regular
    editor:
      type: CLion
components:
  - name: rust-container
    container:
      # image from Docker Hub
      image: rust:latest